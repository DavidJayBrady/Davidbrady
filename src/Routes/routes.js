import React from 'react';
import {BrowserRouter, Route, IndexRoute} from 'react-router-dom';

import App from '../App';

export default (
	<BrowserRouter>
		<Route component={App} >
		</Route>
	</BrowserRouter>
)